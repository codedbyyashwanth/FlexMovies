

const Overlay = ({name, top}) => {
        return (
                <div className={`${name} absolute ${top} left-0 right-0 w-full h-1/4`}>
                        
                </div>
        );
}

export default Overlay;